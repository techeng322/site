import { PageLayout } from '../PageLayout';

const NoAddressState = () => (
	<PageLayout>
		<div className="max-w-4xl mx-auto space-y-16">
			<h1 className="font-size-lg text-center">Wallet not connected</h1>
		</div>
	</PageLayout>
);

export default NoAddressState;
